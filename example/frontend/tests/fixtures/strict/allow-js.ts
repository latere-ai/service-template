// allowJs off: a JavaScript module is not part of the program, so importing
// one is a type error rather than a silent shadow.
import { help } from "./helper.js";

export const value: number = help();
