# The workload runs under its own account with no token mounted. The service
# does not call the API server, and a mounted token is a credential an
# attacker inside the container can use.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Name }}
automountServiceAccountToken: false
